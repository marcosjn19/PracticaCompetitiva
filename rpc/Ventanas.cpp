#include <bits/stdc++.h> 
#include <algorithm>
using namespace std;

void solve (int n, int k, vector<int>tamanos) {
    int size = n - k + 1;
    vector<pair<int, int>>pares;
    int aux;
    for ( int i = 0; i < size; i++ ){
    }
    return;
}

int main(){
	//freopen("input.txt", "r", stdin); 
	//freopen("output.txt", "w", stdout);  
    int n; cin >> n;
    int k; cin >> k;
    vector<int>numeros;
    string s;
    while ( n-- ){
        int num; cin >> num;
        numeros.push_back(num);
    }
    solve(n, k, numeros);
}